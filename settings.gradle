rootProject.name = 'soffa-platform'

['soffa-core',
 'soffa-web',
 'soffa-data-jpa',
 'soffa-rabbitmq',
 'soffa-security',
 'soffa-tests',
].each {
  include ":${it}"
  project(":${it}").projectDir = file("src/${it}")
}


